# Kōan Docker — Environment Variables
#
# Copy this file to .env.docker and fill in your values.
# This file is loaded by docker-compose.yml via env_file.

# =========================================================================
# TELEGRAM (required for Telegram integration)
# =========================================================================
# Get these from @BotFather on Telegram
KOAN_TELEGRAM_TOKEN=
KOAN_TELEGRAM_CHAT_ID=

# =========================================================================
# OPTIONAL: GitHub CLI identity
# =========================================================================
# If set, gh CLI commands will run as this user.
# The user must be pre-authenticated on the HOST (gh auth login).
# GITHUB_USER=your-bot-name

# =========================================================================
# OPTIONAL: Docker build args (also settable in docker-compose.yml)
# =========================================================================
# Match these to your host user to avoid permission issues on mounted volumes.
# macOS default: UID=501, GID=20
# Linux default: UID=1000, GID=1000
# HOST_UID=501
# HOST_GID=20
